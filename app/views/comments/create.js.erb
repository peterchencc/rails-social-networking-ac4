<% if @comment.valid? %>
  $('#timelineComments-area-<%= @photo.id %>').append("<%= j(render :partial => 'comment', :locals => { :p => @photo, :c => @comment } ) %>");
  $(".comment-form form").trigger("reset");
<% else %>
  alert("comment failed!");
<% end %>